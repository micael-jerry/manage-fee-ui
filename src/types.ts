export type StudentType = {
    id: number,
    ref: string | null,
    lastname: string,
    firstname: string | null,
    sex: string,
    birthDate: string,
    address: string,
    phone: string,
    email: string,
    entranceDate: string
    role: string
}
